@Html.DevExpress().Spreadsheet(
    settings => {
        settings.Name = "spreadsheet";
        settings.CallbackRouteValues = new { Controller = "Features", Action = "PivotTablesPartial" };
        settings.Width = Unit.Percentage(100);
        settings.Height = 700;
        settings.ShowConfirmOnLosingChanges = false;
        settings.PreRender = (s, e) => {
            MVCxSpreadsheet spreadsheet = (MVCxSpreadsheet)s;
            SpreadsheetDemoUtils.HideFileTab(spreadsheet);
            spreadsheet.Document.PivotCaches.RefreshAll();
        };
}).Open(Path.Combine(DirectoryManagmentUtils.CurrentDataDirectory, "PivotTableDemoTemplate.xlsx")).GetHtml()